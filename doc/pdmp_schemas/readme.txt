Included:

medication.xsd -- granular medication schema aligned with PDMP
patient.xsd    -- granular patient demographic schema

root.xml       -- hData root xml for PDMP
			note: schema locations need to be aligned with actual location

medication.xml -- granular sample xml instance for medication entry
patient.xml    -- granular sample xml instance for patient demographics 

record.xsd     -- composed pdmp report schema
composedPDMP.xml -- sample composed PDMP xml report with single entry

data_types.xsd -- supporting data type schema
